{"session":"nolan-check","action":"evaluate","code":"JSON.stringify({title:document.title,body:(document.body.innerText||'').slice(0,400),inputs:Array.from(document.querySelectorAll('input,textarea')).map(function(e){return e.tagName+':'+(e.name||e.placeholder||'').slice(0,30)}).slice(0,8),editors:Array.from(document.querySelectorAll('.CodeMirror,[contenteditable=true],iframe')).map(function(e){return e.tagName+'.'+(e.className||e.id||'').toString().slice(0,40)}).slice(0,6)})"}
