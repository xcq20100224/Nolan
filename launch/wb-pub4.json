{"session":"nolan-check","action":"evaluate","code":"(function(){var btns=[];document.querySelectorAll('a,button').forEach(function(el){var t=(el.textContent||'').trim();if(t&&t.length<15&&/写|开始|知道|进入|继续/.test(t))btns.push(t+' <'+el.tagName+' href='+(el.href||'')+' >')});return JSON.stringify(btns.slice(0,10))})()"}
