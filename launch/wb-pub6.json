{"session":"nolan-check","action":"evaluate","code":"JSON.stringify({url:location.href,inputs:Array.from(document.querySelectorAll('input,textarea')).map(function(e){return (e.tagName+':'+(e.name||e.placeholder||e.className).toString().slice(0,40))}),editors:Array.from(document.querySelectorAll('[contenteditable],.CodeMirror,[class*=editor]')).map(function(e){return e.tagName+'.'+(e.className||'').toString().slice(0,50)}).slice(0,8)})"}
