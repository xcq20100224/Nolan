{
  "_说明": "T3 确认梯度示例配置。自 T3 起，三档梯度（直接做/做完汇报/先问一句）已内置在 auth_policy.py 的代码数据表中，策略文件缺失也照常生效；同目录的 auth_policy.json 是内置梯度的镜像，可直接在其上追加自定义规则。本示例演示如何追加：用户黑名单优先级最高，其次内置 confirm，再是用户白名单，最后内置 auto，未命中一律 default。注意：本策略只管「确认流程」，hands/VLM 里硬编码的密码/支付/删除安全禁令在任何策略下都不会被解除。",
  "_whitelist示例": "在 auth_policy.json 的 whitelist 数组里追加如下规则即可扩展「直接做」范围（只放你真正信任的低风险操作）：",
  "whitelist追加示例": [
    {
      "_注释": "记事本里的界面操作（打字、保存等）免确认",
      "tool": "gui_control",
      "app_pattern": "记事本|notepad"
    }
  ],
  "_blacklist示例": "在 auth_policy.json 的 blacklist 数组里追加如下规则即可扩展「先问一句」范围（黑名单优先级恒高于一切白名单）：",
  "blacklist追加示例": [
    {
      "_注释": "涉及密码的任何 shell 命令永远确认",
      "tool": "run_shell",
      "cmd_pattern": "密码|password"
    }
  ]
}
