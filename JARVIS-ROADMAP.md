# JARVIS 路线图 v2 · 从 Nolan 到电影中的贾维斯

> **使命（先于一切路线）：Nolan 是数字时代每个人应有的伙伴。**
> **用户不用，坚决不打扰；要用时，绝对出手。**
> 每个阶段只问一句：它让「敢托付」的范围扩大了一寸吗？

> 第一性原理纲领：
> 贾维斯 = 模型能力 × 系统可靠性 × 上下文（记忆/工具/权限）。
> 任务成功率 = 单步成功率 ^ 步数；信任 = 承诺与兑现的一致性在时间上的累积。
> 每一项都做 measurable 验收，不做没有验收数字的阶段。

## 已落成（2026-08-02，全部实测）

- 执行闭环复核（每步 expect→截屏复核→换策略；done 需复核）
- UIA 按名定位点击 + 前台保障 + 遮挡误报恢复
- 技能固化：学会一次永久会做（重放 2.4 倍速）
- 结构化记忆（偏好/习惯/工作/人际/事实）
- 主动晨报 + 闹钟双通道必达 + 唤醒词耳蜗（说「诺兰」即应）
- 声音单通道 + 可打断 + 管家语速
- 度量：高考题库 57 题 100% + 真实任务基准 20 题 95%

## 改进先后顺序（P0 → P6，按杠杆排序）

### P0 · 真人使用闭环（即日开始，持续）
**干什么**：主人每天用 Nolan 干 3 件真事，失败原样摔回来。
**物理原因**：90%→99.9% 的可靠性只能从真实失败里长出来，
闭门题库测不到真实世界的分布。这是唯一不能委托给 AI 的工作。
**验收**：每周真实失败数下降；技能库每周净增 ≥ 3 条。

### P1 · 分层规划（长任务大脑，1-2 周）
**干什么**：>5 步任务先出宏观计划（3-7 个子目标），每个子目标进执行闭环；
子目标失败只重规划该子目标，不从头再来。执行记忆（中间产物、界面状态）
写入工作记忆，重规划带完整上下文。
**物理原因**：当前 >4 步任务是脆弱的（步数超限、目标漂移），
这是「成功率^步数」里 n 变大的根本对策——把大 n 切成小 n 的乘积。
**验收**：10 步以上长任务成功率 ≥ 60%；基准不掉。

### P2 · 技能泛化与技能库管理（1 周）
**干什么**：技能参数化（「播放我喜欢第 N 首」N 是变量，一类任务一条技能）；
前端技能库面板（查看/删除已固化技能）；误固化技能一键遗忘。
**物理原因**：逐字匹配的技能覆盖面太窄，参数化让一条技能服务一类任务；
不可见的技能库不可信——同伴会的东西必须对主人透明。
**验收**：参数化技能命中变体任务；面板可增删。

### P3 · 全双工（边说边听，2-3 周，独立工程）
**干什么**：唤醒后进入对话态：Nolan 播报时主人可随时插话打断并接着说；
告别按键说话。
**物理原因**：这是「在场感」的最后一块拼图，也是工程上最硬的一块
（回声消除、双工抢麦、误触发三重调优），所以排在可靠性工作之后。
**验收**：播报中插话打断成功率 ≥ 90%；误唤醒每天 ≤ 1 次。

### P4 · 条件触发自动化（1-2 周）
**干什么**：「每天下午六点提醒我站起来」「每周五下午把本周日报整理好」——
周期/条件任务注册、触发、执行、汇报。
**物理原因**：主动智能从「定时问候」升级为「按主人的规则主动干活」，
这是信任复利最强的方向（每次准点兑现都是一次承诺-兑现）。
**验收**：3 个周期任务连续一周零漏触发。

### P5 · 模型换代接入流程（每代一次，每次 ≤ 1 小时）
**干什么**：智谱发新模型（GLM-5 系）时，大脑+视觉双配置十分钟切换，
跑基准对比，达标即切。
**物理原因**：模型能力是全世界免费帮我们推进的变量，
接入成本越低，浪潮利用率越高。
**验收**：新模型接入后基准不降，长任务成功率提升。

### P6 · 多设备与开源社区（时机成熟后）
**干什么**：手机端指令入口；README 英文版 + demo 视频更新 + 社区运营。
**物理原因**：杠杆从「一个人用」到「一群人用」——
但没有 P0-P4 的可靠性厚度，传播就是放大缺陷。
**验收**：100 个真实用户，周留存 ≥ 30%。

## 永不做的事（砍单）

- 不做与可靠性无关的 UI 美化
- 不在 P0-P2 完成前做 P6 传播
- 不为想象中的需求写代码（OCR 融合等，等实测故障出现再说）
- 不重复造轮子：有 API 不用 GUI 自动化，有现成模型不自训
